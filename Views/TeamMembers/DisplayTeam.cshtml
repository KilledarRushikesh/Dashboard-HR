@model IEnumerable<ThinkBridge.Models.TeamDetails>

@{
    ViewBag.Title = "DisplayTeam";
}

<h2>DisplayTeam</h2>

<p>
    @Html.ActionLink("Create New", "AddTeam")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TeamName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TeamLeader)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProjectLead)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TeamName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TeamLeader)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectLead)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeamsDetailsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeamsDetailsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeamsDetailsId })
        </td>
    </tr>
}

</table>
